// Public entry point for the standalone finance module.
// Keep external imports pointed at this barrel (e.g. `import('../modules/finance')`)
// so future internal restructuring (tabs/, modals/, hooks/, integrations/) stays
// invisible to the rest of the app.
export { default } from './FinancePanel'
